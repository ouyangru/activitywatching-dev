# Bug History · 发布与线上运行

记录 Aliyun、Nginx、systemd、一键发布、HTTPS、生产版本同步、静态资源上线与线上运行相关的**已验证解决**问题。

特别注意：此模块只有在确认生产环境实际运行的 commit / 构建版本与预期一致，并从浏览器或线上接口验证新行为后，才能记录为已解决。

> 模块化前的历史记录见 [`legacy-chronological.md`](legacy-chronological.md)。新条目只追加，不改写旧条目。
