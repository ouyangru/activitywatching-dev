# 前端结构与验证

## 页面与数据

- `/` 总览：日期切换、分类与目的扇形图、综合/设备全天时间轴、分类联动、设备筛选、应用排行及活动修正。
- `/daily` 日报：同一套主题和日期导航，叙述摘要、基于活动记录的复盘入口、分布、排行、打断来源、记忆展示与人工记录管理。
- `/compare` 多日对比：最近 7 天或自选 1—14 天，分类/目的堆叠柱图、按天对齐的全天时间轴、可点击日期与逐日明细。

日期使用服务端返回的统计时区。页面间导航保留 `day`；对比页额外保留 `start`、`end`。
历史查询使用已有 `day` 参数。对比页仅访问 summary 与 timeline，不调用触发日报生成的接口，并将并发限制为两个日期。
今天只有已过去的时长；空闲、未判定、无设备记录仍分别呈现，不按“浪费”或“效率下降”解释。

## 模块

- `backend/static/design.css`：共享视觉变量、布局、图例、响应式、焦点态与动效，在原有样式后加载以兼容既有页面。
- `backend/static/ui.js`：统计时区、日期校验/导航、请求、图表实例复用、统一扇形图、区间修正弹窗、可追溯的复盘提示。
- `app.js` / `daily.js` / `compare.js`：各页面的数据装配与交互。

新增图表优先复用 ActivityUI.chart / pie，分类颜色从 ActivityUI.colors 取得；需要新的页面时沿用导航和日期参数。
图表有数据更新过渡，选中记录有定位反馈，按钮有按压反馈；遵守 prefers-reduced-motion，后台标签不轮询总览。
加载失败时隐藏过期内容并提供重试，较旧的日期请求不会覆盖新日期结果；图表不可用时仍显示数值明细。

## 人工修正

空闲和无设备记录可补为学习、工作、娱乐或生活活动；允许调整起止时间，因此可以把一小时分别补为 40 分钟学习和 20 分钟休息。
仅覆盖空闲/缺失区间，活跃设备记录保留。新增覆盖记录不会删除之前的记录；日报的人工记录管理可删除对应记录及关联习惯以撤销。
“让系统参考”默认不选，人工分类优先于自动判断。学习/工作按相应目的统计，不计入原空闲应用的使用时长。

复盘提示采用明确、可定位的活动依据，不是新增的大模型评价系统；不把 IDE 打开时长等同于任务产出。

## 已验证

```bash
python -m unittest tests.test_offline_corrections_unit -v
node --test tests/frontend_unit.test.cjs
node --check backend/static/ui.js
node --check backend/static/app.js
node --check backend/static/daily.js
node --check backend/static/compare.js
python -m compileall -q backend/app
```

4 项 Python 逻辑测试和 6 项 JavaScript 逻辑测试通过，另检查 HTML 重复 ID、本地脚本引用与 diff 空白错误。
JavaScript 测试使用 VM 与小范围 DOM/chart doubles，覆盖时区日期、图表实例复用、历史参数、请求乱序、失败隐藏和对比 tooltip。

本次环境无法安装完整 FastAPI/pytest 依赖，也无法访问浏览器本地预览；浏览器内联测试另受自动审查大小限制。因此尚未完成完整 API 回归、浏览器端到端与视觉验收，不能把逻辑测试等同于这些验证。
部署前在正常开发环境安装 `backend/requirements-dev.txt` 后运行 `pytest tests/ -q`，并检查：

1. 在总览切到历史日期，切设备/视图，点击扇形图图例定位相关分类，再跳日报；日期应保持一致。
2. 把空闲的一部分改为学习，再将另一部分改为工作；总览、日报目的占比、多日明细应一致，删除人工记录后恢复。
3. 快速切换不同日期，断开后端后重试，确认不存在旧数据被冠以新日期的问题。
4. 对比 7 天和 14 天，点击柱条/时段回到当天；含今天时明确标记“进行中”。
5. 检查 390px 与 1440px 宽度、键盘焦点、长设备名、多设备图表以及减少动态效果设置。
