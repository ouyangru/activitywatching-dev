# 后端性能优化说明（2026-09）

> 本文写给项目维护者：不讲实现细节，只讲「原来慢在哪、现在怎么快了、为什么不用担心行为变化」。

## 背景：为什么需要优化

系统有两条一直在跑的路径：

1. **写入路径**：采集器每 30 秒上报一次 `/api/v1/events/batch`，服务端要把这一天的片段全部重建一遍（规则底账 + 跨设备合并）。
2. **读取路径**：前端页面会轮询 `/timeline`、`/summary`、`/daily/report` 等接口，每次请求都要查询、合并、序列化当天的数据。

问题在于：**随著一天时间推移，片段越攒越多，而旧代码里最核心的合并算法耗时是「片段数的平方」**。白天开头几十段毫无感觉；到了晚上积累到上千段（多设备 + 线下活动拆分后可达 2000+），每次请求就要几百毫秒到秒级的 CPU，采集器每 30 秒的写入也被拖慢。

## 优化前后对照

用「一天 962 个片段、两台设备」的合成数据实测（同一台机器、改动前后代码对比）：

| 指标 | 改动前 | 改动后 | 提升 |
| --- | --- | --- | --- |
| 跨设备合并算法（2518 段直接调用） | 0.481 秒 | 0.039 秒 | **12.3 倍** |
| 日报接口 `/daily/report`（804 段） | 152 毫秒 | 87 毫秒 | 1.75 倍 |
| 时间线接口 `/timeline/today`（804 段） | 86 毫秒 | 66 毫秒 | 1.3 倍 |
| 全天数据写入总耗时（12 批） | 2.81 秒 | 2.10 秒 | 1.34 倍 |

更关键的是**增长曲线**：改动前的耗时随片段数平方增长（晚上越来越卡），改动后基本是线性（多少数据都是毫秒级）。片段再多也不会恶化。

## 改了什么（通俗版）

按收益从大到小排列：

### 1. 合并算法：从「每个时间段都翻一遍全部数据」改成「按时间顺序扫一遍」

旧算法每处理一个小时间段，都要把当天所有片段从头到尾检查一遍看谁重叠——片段 1000 个就是 100 万次比较，2000 个就是 400 万次。新算法按时间顺序前进，片段开始时加入、结束时移出，每个时间段只看真正重叠的几条。

### 2. 查数据库不再「把用不到的大字段也拖出来」

补齐「无设备时段」时只需要每条记录的开始时间和时长，旧查询却把窗口标题、剪贴板 JSON 等所有列都读出来。新增了只取两列的专用查询，每个读取接口每次请求都省一次全列扫描。

### 3. Agent 未配置时不再白算缓存版本号

每次读取请求都会经过 Agent 覆盖层，旧代码即使完全没配 LLM，也会每次做一遍「全表扫描 + 哈希」来算记忆版本号——算出来的东西根本用不上。现在未配置时直接跳过。配置了 Agent 的情况下，这个版本号也改为进程内缓存，只在记忆真正变化时重算。

### 4. 数据库写入：逐条插入改批量插入

一批 1000 条数据原来是 1000 次单条 SQL 执行，现在一次批量执行完成。同时给数据库加了两个常用查询的索引（按设备 + 时间查），并把 SQLite 设为 WAL 模式的标准搭配 `synchronous=NORMAL`，每次写入的磁盘同步次数大幅减少。

### 5. 删掉重复代码和无效查询

- 日报生成时会先查一遍数据库、然后立刻被另一份数据覆盖——白查 6 个全天数据，删掉。
- 主程序里三段一模一样的「时间格式转换」代码合并成一个函数。

## 为什么不用担心行为变化

这次是**纯性能优化**，对所有接口的返回结果承诺逐位一致：

- 合并算法改写后，用 3000 组随机生成的多设备数据做了**新旧实现逐字节对比测试**，输出完全一致；
- 全量测试套件（72 个，含合并算法的固定样本测试、去重计数、Agent 关闭行为）改动前后全部通过；
- 合成数据灌入一整天后，新旧代码产出的片段数量完全相同（962 = 962）。

架构红线（规则底账、Agent 只写派生表、隐私脱敏、人工修正优先）全部未触碰。

## 部署注意

- **无需数据迁移**：新索引在建库时自动创建，对已有数据库会在下次启动时自动补上（`CREATE INDEX IF NOT EXISTS`）。
- **无需改配置**：不涉及任何环境变量、表结构、接口格式的变化。
- 优化只改了 5 个文件：`backend/app/` 下的 `merger.py`、`database.py`、`agent.py`、`summarizer.py`、`main.py`。

## 如何验证

```bash
# 全量测试（应全绿）
python3 -m pytest tests/ -q

# 冒烟：启动服务后访问几个读取接口确认 200
uvicorn backend.app.main:app --port 8765
```
