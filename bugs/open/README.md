# Open Bugs

这里记录尚未完成“复现 / 根因 / 修复 / 验证 / 线上确认”闭环的问题。

规则：

- 未解决问题不要提前写进 `bughistory/`。
- 每个问题单独一个 Markdown 文件，文件名使用简短 slug。
- 问题解决后，把最终结论整理到对应模块的 `bughistory/*.md`，open 记录可删除或改为指向已解决条目。
- 对线上问题，GitHub commit 存在不等于问题已解决；必须核验实际部署版本和线上行为。
