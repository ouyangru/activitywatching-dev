# 固定 HTTPS 云端部署（Render）

## 服务与费用

`render.yaml` 创建一个新加坡区域的付费 Python Web 服务和 1 GB 持久磁盘。平台提供固定 `https://<实际分配名称>.onrender.com` 网址并管理 HTTPS 证书，无需购买域名。计算资源、磁盘和可能的额外用量以创建页面的账单为准。本配置尚未创建任何云资源。

持久磁盘是独立于程序发布版本的数据存储。数据库放在 `/var/data/activitywatch.db`，重新部署仍保留；不要删除磁盘或使用 `free` 方案替换。SQLite 使用单实例、单工作进程，不配置多副本。带磁盘的重新部署可能短暂中断服务，客户端通过离线队列重试。

官方依据：https://render.com/docs/web-services 、https://render.com/docs/disks 、https://render.com/docs/blueprint-spec

## 首次部署

先将本次代码审阅并提交到你的远程 Git 仓库（包括 `render.yaml`、backend 和静态页面）。在 Render 登录，选择 New → Blueprint，授权目标仓库并选择包含本次改动的分支；确认区域、磁盘和费用后创建。等服务显示 Live，保存平台给出的 HTTPS 地址。

在服务 Environment 中查看平台自动生成的 `ACTIVITYWATCH_API_TOKEN`，妥善保存。该值是访问和上传数据的凭证，重新部署时使用同一个值；不使用本地 `start-mobile.ps1` 生成的临时令牌。修改云端令牌后需要更新所有采集器并重新登录网页。

使用手机移动数据打开 `https://<实际网址>/mobile`，页面会跳转登录；输入令牌后查看状态。生产模式关闭 URL 令牌登录，使用 HTTPS 登录表单；浏览器 Cookie 标记 Secure、HttpOnly，七天过期。只有登录页和健康检查公开可见，数据接口均要求认证。

## 连接采集器与验收

Android 应用后端地址填写 `https://<实际网址>`（不附加 `/mobile` 或 `?token=`），令牌填写云端生成值。保存后点击立即同步。Windows 采集器启动参数为 `--server https://<实际网址> --token <云端令牌>`。两个采集器已有 HTTPS 支持，无需改变数据格式。

云端新建数据库最初为空；电脑当前数据库中的历史记录不会自动迁移。切换前请保留原数据库。手机已经确认上传并从离线队列删除的历史也不会自动再传。需要迁移时，先用 SQLite 在线备份得到一致的备份文件，再在云端停写期间导入；不要直接复制正在写入的 `.db` 而忽略 WAL 文件。

上线验收：关闭手机 Wi-Fi，仅用移动数据执行立即同步，然后在网页确认该设备与刚产生的片段；关闭电脑后重复手机同步，以确认后端已独立运行。后台同步仍由 Android 调度（约 15 分钟，可能延迟），云端部署不等于秒级采集。还需从你的实际运营商网络验证域名可访问性。

持久磁盘不是异地备份。正式长期使用前，在平台 Shell 使用 Python sqlite3 的 `Connection.backup()` 导出一致快照并保存到独立存储；定期检查容量和恢复可用性。不要把平台磁盘快照直接覆盖到正在运行的 SQLite 服务上。
