这个应用是可以落地的，而且真正有价值的地方并不是简单统计“某个软件打开了多久”，而是把手机、电脑上的碎片化行为合并成一条时间线，最后告诉你：今天有多少时间在学习、工作、娱乐，有哪些时间被频繁切换和无意识刷手机消耗了。



整体可以设计成下面这条链路：

flowchart LR
    A["电脑采集器"] --> C["统一事件模型"]
    B["手机采集器"] --> C
    C --> D["活动识别与合并"]
    D --> E["时间线与日报"]

最小可用版本建议先做“电脑端 + Android 端”，不要一开始追求所有设备，因为 iPhone 对后台使用情况的开放程度比较低，而电脑和 Android 更适合做实验。



电脑端每隔几秒记录当前前台应用、窗口标题、浏览器域名以及键鼠是否空闲，例如识别出“VS Code：mini-nccl”“Chrome：CUDA Programming Guide”“Bilibili：某视频”。

Android 端主要记录前台应用及使用区间，第一版只判断微信、浏览器、B站、LeetCode 等应用的使用时间，不要急着读取聊天内容或者截图。

浏览器最好单独做一个扩展，因为只知道打开了 Chrome 没有什么意义，必须进一步区分 GitHub、技术文档、招聘网站和视频网站。

所有设备把数据转换成统一事件，例如 开始时间、结束时间、设备、应用、标题、域名、是否空闲，再上传到本地服务器或者电脑上的 SQLite 数据库。

分类模块先使用规则，将 GitHub、VS Code、终端归到“学习/开发”，将公司软件归到“工作”，将游戏和短视频归到“娱乐”；遇到标题模糊的事件，再让模型辅助判断，而不是把所有隐私数据直接交给模型。



这里最麻烦的其实不是采集，而是“时间应该怎么算”。例如你电脑正在播放课程，同时拿起手机刷了十分钟视频，如果直接累加就会得到二十分钟，因此需要根据键鼠操作、手机前台状态和事件优先级判断主要活动；此外，应用类别也不能完全写死，因为你在 B 站看 CUDA 教程属于学习，看娱乐视频才属于娱乐。



第一版可以只实现四个结果：一天的时间线、学习/工作/娱乐占比、应用使用排行、最长连续专注时间。技术上可以采用：

电脑采集器：C++ 或 Python
Android采集器：Kotlin
本地服务：FastAPI
数据库：SQLite
展示页面：Vue/React + ECharts
智能分类：规则优先，LLM 只处理无法判断的标题

按照你的经历，我更建议把它做成一个偏系统工程的“本地优先数字活动追踪器”：采集器常驻运行，离线保存，设备间增量同步，敏感标题可以在本地脱敏，用户还能随时删除原始记录。这个方向既能解决你自己的时间管理问题，也能展示后台服务、事件采集、跨设备同步、数据库、隐私控制和智能分类，作为项目会比单纯做一个网页统计器完整得多。



实际开发时，第一阶段只做电脑端：采集前台窗口、识别空闲状态、写入 SQLite、生成当天时间线；等这条链路稳定后再接 Android，否则很容易同时卡在权限、同步和分类三个问题上。真正需要做的不是把每一次按键和点击直接解释成行为，而是先把它们压缩成“某段时间内的交互特征”，再结合当前应用、页面内容和前后行为，判断你是在阅读、编程、写作、沟通还是娱乐。



可以采用四层转换：

原始事件 → 时间窗口特征 → 连续行为片段 → 学习/工作/娱乐

例如每 30 秒汇总一次数据，再把相邻且语义一致的窗口合并成一个完整片段：

原始数据建议保留的特征可以辅助判断的行为

键盘

输入次数、持续时间、退格比例、快捷键类型，不保存具体文字

写作、编程、搜索、聊天

鼠标

点击频率、滚动距离、停留时间、点击控件类型

阅读、浏览、视频观看、软件操作

剪贴板

复制/粘贴时间、内容类型、长度区间、来源和目标应用

整理资料、复制代码、搬运文本

活动窗口

进程、脱敏后的标题、浏览器域名、持续时间

判断行为发生的上下文

空闲状态

最近一次键鼠操作、屏幕锁定和媒体播放状态

区分真正使用、挂机和看视频

其中最重要的是尽量不要记录原始键盘内容，因为“记录输入了什么”很容易把密码、聊天和公司数据全部保存下来，其实判断行为通常只需要知道你是否持续输入、输入发生在哪个应用以及输入节奏。剪贴板也可以只记录类型和长度；如果确实需要理解语义，可以在本地完成分类，例如判断为“代码、网址、普通文本”，随后立即丢弃原文。



窗口阅读也不一定要不断截图，可以优先读取当前应用、窗口标题、浏览器域名以及界面控件类型；如果以后需要理解页面内容，再做成明确开启的本地 OCR 功能，并针对密码管理器、银行、聊天软件和无痕窗口建立排除名单。



行为识别最好拆成两个问题：



你正在做什么：阅读、编程、写作、搜索、沟通、观看视频或者空闲。

这件事属于什么目的：学习、工作、娱乐或者生活事务。



因为“阅读”本身不能说明目的，例如阅读 CUDA 文档可能属于学习，阅读公司需求文档属于工作，阅读小说则属于娱乐。第一层可以通过交互模式判断，第二层再结合项目路径、网站类别、窗口标题和你的个人规则判断。



例如下面这组事件：

VS Code 前台 18 分钟
键盘持续输入，期间运行终端命令 4 次
浏览器打开 CUDA 文档 6 分钟
VS Code 与文档之间切换 7 次
手机基本未使用

最终可以合并为：

20:30—20:56
行为：编程与查阅技术资料
目的：学习
主题：CUDA / Mini-NCCL
专注度：较高
置信度：92%

第一版不需要直接上复杂模型，可以先建立规则：IDE 加持续输入判断为编程，PDF 或网页加低频滚动和长时间停留判断为阅读，播放器加媒体播放和低交互判断为观看视频，聊天软件加短促输入和频繁切换判断为沟通。系统同时展示“为什么这样分类”，并允许你一键纠正，积累一段时间后再使用这些人工标签训练个人化分类器，这会比一开始让大模型猜测所有行为可靠得多。我建议先把它定义为一个“跨设备自动行为日记”，第一版默认采用 Windows + Android：电脑采集活动窗口、浏览器页面和键鼠活跃度，手机采集前台应用与屏幕使用区间，然后在本地将事件合并成“编程、阅读、沟通、视频、娱乐、空闲”等连续行为，最后再归入学习、工作、娱乐和生活四类。



最小版本只需要完成这条链路：

设备采集器 → 本地脱敏与30秒聚合 → SQLite存储
→ 行为片段识别 → 跨设备时间合并 → 当日时间线与占比

第一阶段不要保存具体按键、剪贴板原文或连续截图，只保留输入频率、复制粘贴次数、活动应用、脱敏标题、浏览器域名和空闲状态；这样已经足够判断多数行为，也不会让项目演变成一个危险的个人键盘记录器。



如果你的实际设备不是 Windows + Android，而是 Linux、macOS 或 iPhone，告诉我具体组合，我就可以继续按照对应平台给你拆出采集接口、数据结构和第一版代码目录。